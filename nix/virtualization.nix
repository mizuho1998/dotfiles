{ config, pkgs, lib, ... }:

{
   # home.packages = with pkgs; [
   #   docker
   #   docker-compose
   #  ];
  # programs.home-manager.useUserPackages = true;
  # virtualisation = {
  #   docker = {
  #     enable = true;
  #     enableOnBoot = true;
  #   };
  # };
  # environment.systemPackages = [
    # pkgs.docker-compose
  # ];
}
